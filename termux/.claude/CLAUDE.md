# CLAUDE.md

# MUST

- 回答は日本語で行ってください
- この環境は Termux (Android) です
- パッケージインストールには `brew` ではなく `pkg` を使用してください
- グローバルなバイナリは `/usr/local/bin` ではなく `$PREFIX/bin`（`/data/data/com.termux/files/usr/bin`）に配置されます
- 作業が終了したらユーザーに完了を伝えてください
